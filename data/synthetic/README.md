# Synthetic corpus (samples)

Two sample document sets are kept here (`*_0`, `*_1`) plus the computed
`ocr_ablation_results.json`. Regenerate the full 24-document corpus with:

    make data      # or: python scripts/generate_synthetic_docs.py --n 24 --severity 0.9
